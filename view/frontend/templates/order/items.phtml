<?php
/** @var \Tigren\CompanyAccount\Block\Order\Items $block */

use Magento\Framework\App\Action\Action;

?>

<?php
$_order = $block->getOrder();
$totals = $block->getChildHtml('order_totals');
$payment = $_order->getPayment()->getMethodInstance()->getTitle();
$odItems = $_order->getAllItems();
$_helper = $this->helper('Magento\Catalog\Helper\Output');
$_productCollection = $block->getLoadedProductCollection();
?>
    <div class="order-table-content">
        <div class="order-table-1">
            <table>
                <tr>
                    <th><?= __('Delivery Address') ?></th>
                    <th><?= __('Payment Method') ?></th>
                </tr>
                <tr>
                    <td><?= $block->getFormattedAddress($_order->getShippingAddress()) ?></td>
                    <td><?= $payment ?></td>
                </tr>
            </table>
        </div>
        <div class="order-table-2">
            <table style="margin-bottom: -15px">
                <th><?= __('Payment Method') ?></th>
            </table>
            <?= $totals ?>
        </div>
    </div><div class="clearfix"></div>
    <div class="order-delevired">
        <div class="order-delevired-col-8">
            <h2 class="order-title-delevired">Order Items</h2>
            <div class="" style="width: 100%;clear: both;overflow: hidden;">
                <div class="order-products wrapper">
                    <ol class="order-products order-product-items">
                        <div class="order-product-item-info">
                            <?php foreach ($_productCollection as $_product): ?>
                                <?php foreach ($odItems as $odItem): ?>
                                    <?php $_product = $block->getProductId($odItem->getProductId()); ?>
                                    <div class="box-order-product">
                                        <div class="order-image" style="float: left">
                                            <a href="<?= $_product->getProductUrl() ?>"
                                               class="product photo product-item-photo">
                                                <img class="order-image-product"
                                                     src="<?php echo $block->getProductImageUrl($_product, 'category'); ?>"
                                                     alt="" width="160px" height="auto"> </a>
                                        </div>
                                        <div class="order-product details order-product-item-details">
                                            <div class="order-name order-product-item-name">
                                                <strong class="order-name order-product-item-name">
                                                    <a class="order-product-item-link"
                                                       href="<?= /* @escapeNotVerified */
                                                       $_product->getProductUrl() ?>">
                                                        <?= /* @escapeNotVerified */
                                                        $_helper->productAttribute($_product, $_product->getName(), 'name') ?>
                                                    </a>
                                                </strong>
                                                <span class="order-price">
                                                <?php echo /* @escapeNotVerified */
                                                $block->getProductPrice($_product); ?>
                                            </span>
                                            </div>
                                            <?php if ($_product->isSaleable()): ?>
                                                <?php $postParams = $block->getAddToCartPostParams($_product); ?>
                                                <form data-role="company-tocart-form"
                                                      action="<?php /* @escapeNotVerified */
                                                      echo $postParams['action']; ?>" method="post">
                                                    <input type="hidden" name="product"
                                                           value="<?php /* @escapeNotVerified */
                                                           echo $postParams['data']['product']; ?>">
                                                    <input type="hidden" name="<?php /* @escapeNotVerified */
                                                    echo Action::PARAM_NAME_URL_ENCODED; ?>"
                                                           value="<?php /* @escapeNotVerified */
                                                           echo $postParams['data'][Action::PARAM_NAME_URL_ENCODED]; ?>">
                                                    <?php echo $block->getBlockHtml('formkey') ?>
                                                    <button type="submit" title="<?php echo __('Buy it again'); ?>"
                                                            class="action tocart primary button btn-cart pull-left-none">
                                                        <span class="buy-it-again"><?php /* @escapeNotVerified */
                                                            echo __('Buy it again') ?></span>
                                                    </button>
                                                </form>
                                            <?php else: ?>
                                                <?php if ($_product->getIsSalable()): ?>
                                                    <div class="stock available"><span><?php /* @escapeNotVerified */
                                                            echo __('In stock') ?></span></div>
                                                <?php else: ?>
                                                    <div class="stock unavailable"><span><?php /* @escapeNotVerified */
                                                            echo __('Out of stock') ?></span></div>
                                                <?php endif; ?>
                                            <?php endif; ?>
                                        </div>
                                        <div class="clearfix"></div>
                                    </div>
                                <?php endforeach; ?>
                            <?php endforeach; ?>
                        </div>
                    </ol>
                </div>
                <div class="order-delevired-col-4">
                    <button class="track-package"><a class="action-button"
                                                     href="<?php echo $block->getUrlTracking($_order) ?>">Track
                            Package</a></button>
                    <?php if (!empty($block->isModuleEnabled())): ?>
                        <button class="replace-items"><a class="action-button"
                                                         href="<?php echo $block->getRmaLink($_order); ?>">Return or
                                Replace Items</a></button>
                    <?php endif; ?>
                    <hr>
                </div>
                <div class="clearfix"></div>
            </div>
        </div>
    </div>
<?php if (!$block->isRedirectToCartEnabled()) : ?>
    <script type="text/x-magento-init">
        {
            "[data-role=company-tocart-form], .form.map.checkout": {
                "catalogAddToCart": {}
            }
        }

    </script>
<?php endif; ?>
<script type="text/javascript">
    require(['jquery'], function ($) {
        $(document).ready(function () {
            var maxHeight = 100;
            $(".order-product-item-details").css("max-height", "250px");
            $(".order-product-item-details").each(function(){
                if($(this).height() > maxHeight){
                    maxHeight = $(this).height();
                }
            });
            $(".order-product-item-details").height(maxHeight);
        });
    });
</script>